{
  "name": "Plaid Sync",
  "slug": "plaid_sync",
  "version": "1.0",
  "type": "app",
  "description": "Plaid transaction sync service for Home Assistant",

  "arch": ["amd64", "aarch64", "armv7", "i386"],

  "startup": "services",
  "boot": "auto",

  "ports": {
    "8080/tcp": 8080
  },

  "map": ["app_config"],

  "options": {},
  "schema": {}
}
